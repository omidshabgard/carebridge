# CareBridge Backend

Node.js + Express + TypeScript + MongoDB/Mongoose API.

## Getting Started

Copy `.env.example` to `.env`, ensure MongoDB is available, then install the dependencies and start the development server.

```bash
npm install
npm run dev